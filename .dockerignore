# イメージに含める必要がないもの。COPY . . の対象から除外される。
#
# .github/ と docs/ は除外しない。検査はコンテナ内で実行するため、
# scripts/check-pins.sh が参照するワークフローと docs/setup.md (Nix インストーラの
# 固定値) がイメージ内に存在する必要がある。

.git/
.direnv/
.envrc.local
.work/
result
result-*
*.swp
.DS_Store
