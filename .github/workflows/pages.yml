# docs/ を GitHub Pages へ公開する。
#
# サイトの生成は GitHub が提供する Jekyll をそのまま使う。リポジトリ側に生成器の依存
# (Gemfile 等) を持たないため。したがって生成器の版は固定できないが、対象は文書のみで
# あり、環境の再現性には関わらない。
#
# 依存する外部 action はコミット SHA で固定する。タグは再割り当てが可能であり、
# 固定にはならない。
name: Pages

on:
  push:
    branches: [main]
  # 内容を変えずに再公開したい場合に使う。
  workflow_dispatch:

# Pages への配置に必要な権限のみを与える。
permissions:
  contents: read
  pages: write
  id-token: write

# 配置は同時に 1 つとする。実行中のものは打ち切らない。打ち切ると公開されている内容が
# 中途半端な状態で残りうるため。
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        # actions/checkout v4.2.2
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Pages の設定を取得する
        # actions/configure-pages v5
        #
        # 引数は与えない。static_site_generator と generator_config_file による
        # baseurl の注入は、当該 action が .js / .cjs / .mjs しか扱えないため
        # Jekyll の _config.yml には使えない。baseurl は docs/_config.yml に書く。
        #
        # 本手順を残しているのは、Pages が有効化されていない場合にここで明確に
        # 失敗させるためである。
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b

      - name: サイトを生成する
        # actions/jekyll-build-pages v1
        uses: actions/jekyll-build-pages@44a6e6beabd48582f863aeeb6cb2151cc1716697
        with:
          source: ./docs
          destination: ./_site

      - name: 成果物を登録する
        # actions/upload-pages-artifact v3
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 配置する
        id: deployment
        # actions/deploy-pages v4
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e
